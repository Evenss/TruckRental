<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hdu.truckrental" >

    <!-- To auto-complete the email text field in the login form with the btn_home_user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.hdu.truckrental.map.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Base" >

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="RGCA9DkjGOylX9ldn0VCEHuGPzgAgVWr" />

        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:permission="android.permission.BAIDU_LOCATION_SERVICE"
            android:process=":remote" />

        <activity
            android:name="com.hdu.truckrental.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".driver.DriverLoginActivity"
            android:label="@string/label_driver_login">

        </activity>

        <activity
            android:name=".user.UserRegisterActivity"
            android:label="@string/label_register_user">

        </activity>

        <activity
            android:name=".driver.DriverRegisterActivity"
            android:label="@string/label_register_driver">

        </activity>

        <activity
            android:name=".driver.RuleDriverShowActivity"
            android:label="@string/label_driver_rule">
        </activity>

        <activity android:name=".user.OrderCreateActivity"/>

        <activity android:name=".map.MapLocationActivity"/>

        <activity android:name=".driver.DriverAvailableOrderShowActivity"/>

        <activity android:name=".driver.DriverInfoShowActivity"/>

        <activity android:name=".driver.DriverChangePwdActivity"/>

        <activity android:name=".driver.DriverOrderDetailsShowActivity"/>

        <activity android:name=".driver.DriverRunningOrdersShowActivity"/>
    </application>

</manifest>